#include<stdio.h>
int main(){
    //char st[]={'a','n','s','h','\0'};
    char st[]="ansh"; // same as above
    // for(int i=0;i<=3;i++)
    // {
    // printf("first character is %c\n", st[i]);
    // }

    printf("%s", st);

    

    return 0;
}